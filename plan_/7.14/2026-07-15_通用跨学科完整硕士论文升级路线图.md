# 通用跨学科完整硕士论文升级路线图

**日期：** 2026-07-15  
**目标：** 不把成功限制为 RAG 或计算机领域；系统必须能够为任意学科建立正确的方法论、协调真实研究资源，并在证据充分时完成符合院校要求的完整硕士论文。

## 1. 对现状结论的纠正

阶段 0–7 的真实运行只证明了：系统能在受控计算检索课题中完成一个可复现 pilot 研究报告。它没有证明以下能力：

- 人文解释、史料批判、哲学论证和数学证明；
- 访谈、焦点小组、参与式观察和质性编码；
- 问卷、观察研究、因果推断、临床或社会科学统计；
- 湿实验、设备实验、田野调查、受试者招募和伦理审批；
- 系统综述、范围综述、元分析和证据分级；
- 符合具体院校、专业、篇幅、章节和引用规范的完整硕士论文。

因此，“限定领域可完成”不是最终完成状态。

## 2. 当前跨学科硬缺口

1. Research Contract 把课题默认建模为 empirical hypothesis testing。
2. `task_type` 和 Agent 能力矩阵以软件系统、计算实验为中心。
3. 实验执行器只有检索/RAG 模板，无法表达其他研究方法。
4. 对不能由软件执行的资源，没有统一的责任人、完成凭证和阻塞状态。
5. 伦理审批、参与者同意、隐私、动物实验和受监管材料未成为执行硬门。
6. 人文和理论研究被迫提供 treatment、baseline 和数值 metric，认识论错误。
7. 最终报告没有绑定目标院校的学位论文规范、篇幅和完整章节验收。
8. 现有 benchmark 只覆盖科研诚信和计算实验，没有跨学科完整论文金标准。

## 3. 不可伪造的“通用能力”定义

通用能力不等于软件能够亲自做所有物理动作。对于湿实验、临床、田野和访谈，系统必须：

1. 正确设计并预注册方案；
2. 列出设备、场地、材料、人员、审批和数据要求；
3. 把需要人类或机构完成的动作分派为可审计任务；
4. 在收到真实记录、数据和审批凭证前保持阻塞；
5. 验证输入、执行分析、保留失败和负面结果；
6. 只有全链路通过才形成完整硕士论文。

把缺失资源标记为“需要人工/机构完成”是通用科研系统的正确行为；伪造实验记录不是自治。

## 4. 分阶段升级

### 阶段 8：跨学科研究画像与双层可行性门

- 学科画像：broad field、field、subfield；
- 方法族：quantitative、qualitative、computational、experimental、systematic review、humanities、theoretical、design science、mixed methods；
- 认识论模式：假设检验、估计、探索、解释、证据综合、证明构造、artifact evaluation、理论构建；
- 资源计划：数据、语料、设备、实验室、参与者、软件、专业人员及其真实状态；
- 伦理计划：是否需要、审批状态、审批机构、敏感性和风险；
- 学位规范：院校、专业、语言、字数、章节、引用格式；
- 两个独立结论：`research_ready` 与 `master_thesis_ready`。

验收：至少八种方法族可被正确建模；解释性和理论研究不被强迫生成统计假设；缺资源/审批的课题 fail closed；论文规范缺失时只能输出 research report，不能宣称完整硕士论文。

### 阶段 9：通用研究工作包和方法执行器

- 用 research design、data acquisition、analysis、synthesis、thesis assembly 替代计算机中心任务语义；
- 定量：抽样、功效、测量、缺失值、稳健性、效应量与统计假设检查；
- 定性：招募、访谈提纲、转录、编码本、双人编码、饱和度、反身性和负例；
- 系统综述：协议注册、数据库、去重、双人筛选、质量评价、PRISMA、元分析；
- 人文：一手史料、版本/出处、文本批判、解释框架、反论证与互证；
- 理论：定义、引理、证明依赖、反例搜索、形式或独立检查；
- 实验：计算实验、设备/湿实验的人机执行包与原始记录验证；
- 混合方法：并行/顺序设计、联合展示和整合推论。

### 阶段 10：完整硕士论文装配与院校约束

- 章节级论证图和字数预算；
- 研究问题—方法—结果—讨论闭环；
- 表格、图、附录、数据/代码/材料声明；
- 引文样式与参考文献一致性；
- 摘要、关键词、目录、术语、跨章一致性；
- 院校模板和盲审要求；
- 独立导师、方法专家、统计/质性专家与诚信审查。

### 阶段 11：跨学科真实端到端验收

至少完成并保存下列不同范式的真实项目：

1. 计算/工程实证；
2. 社会科学定量或公开数据观察研究；
3. 系统综述；
4. 人文解释或理论研究；
5. 质性或混合方法项目（需要真实参与者时允许经审计的人机协作）。

每个项目必须有真实来源、方法专用质量门、原始材料、完整论文、独立审查和可追溯清单。任何一个范式只能生成模板、虚构数据或缺少关键章节，都不能宣称达到最终目标。

## 5. 最终停止条件

只有同时满足以下条件才停止：

- 不依赖领域关键词硬编码来决定研究有效性；
- 八类方法画像和方法专用质量门均有正反例基准；
- 至少五个不同范式的真实端到端项目通过；
- 有外部资源的项目能正确协调人类/机构并验证返回材料；
- 完整硕士论文规范、章节、篇幅、证据、方法和审查全部通过；
- 系统对无法完成的环节保持阻塞，绝不通过编造文献、数据、审批或实验来“完成”。

在这些证据出现前，目标保持进行中。

## 6. 执行状态

- 阶段 8：已完成跨学科研究画像、资源/伦理门和论文可行性门。
- 阶段 9：已完成通用研究工作包、真实材料哈希和七类确定性分析适配器。
- 阶段 10：已完成完整硕士论文质量门、分章节写作和可追溯装配。
- 阶段 11A：已建立独立五范式真实运行验收器；当前结果为 **1/5**，因此最终目标仍未完成。
- 阶段 11B：计算/工程实证项目 `run_14ee4f7e` 已真实完成并通过独立验收：方法族为 computational，最终有效正文 7335 词（冻结范围 7000–8000），6 个必需章节、6 篇可核验外部来源、8 条受支持结论、可发布实验与独立复现均通过，交付等级为 `master_thesis`。
- 阶段 11C–11D：社会科学定量、系统综述、人文解释、质性研究四个真实项目待逐一执行并通过验收，登记文件为 `benchmarks/thesis_projects.json`。
- 阶段 13U：已根据计算/工程范式首次真实运行修复章节生成死循环风险：补全改为最多两轮、正文专用、长度与错误单调改进，根任务终态后陈旧返工分支自动归档。真实运行已完成文献、研究设计、可复现实验与结果分析，但三个章节因旧补全策略未达 70% 字数硬门，尚未计入通过项目。
- 阶段 13V：论文章节返工改为携带完整上一版 `chapter`，使模型按独立审稿意见定点修改，不再每轮从 summary 重写整章；普通任务继续使用 6000 字符紧凑上下文。
- 阶段 13W：章节独立审稿改为“冻结证据优先、正文在后”的 48000 字符上下文，避免 24000 字符截断恰好删掉 allowed support 和实验工件；对已通过 schema 存在性校验的 support ID，确定性剔除“ID 不存在”式自相矛盾意见，但保留证据与段落语义不匹配的真实问题。
- 阶段 13X：必需根章节任一未通过时，运行必须在论文装配前 fail closed，记录失败章节并停止生成/发布总报告；已被通过新版替代的失败兄弟草稿不影响装配。
- 阶段 13Y：论文 `experiment:*` 支持对象补入冻结实验协议（变量、数据集、指标、基线、方法细节、停止条件和风险），使方法章从真实协议获取“100 字符窗口”等执行事实，不再从结果表猜测切分单位或实验设计。
- 阶段 13Z：论文装配按每个根章节解析最新通过返工版本，忽略历史失败草稿；写作收敛后根章节处于 `blocked`、`failed` 等任何非完成状态均阻断发布，禁止回退到非确定性总稿掩盖章节失败。
- 阶段 14A：修复英文必需章标题大小写导致有效篇幅误计为 0；研究循环把冻结论文契约中的最低受支持 claim 数与不同引用来源数纳入 `ready_to_report`，不足时生成一次合并证据覆盖动作，要求逐来源全文 passage 直接蕴含且禁止用缺失性 context note 凑数。
- 阶段 14B：章节最低篇幅由固定 70% 改为 `max(70%, 院校最低总字数/目标总字数)` 的同比预算，并在装配前复核最新通过版本的实际正文；解决章节分别过门但整篇仍低于院校下限的问题。
- 阶段 14C：受控研究循环的 token/成本预算仅统计 `[循环R*]` 动作及其返工链，不再被初始研究、论文章节和报告审稿消耗误伤；轮次、动作指纹与连续无进展闭锁保持不变，既允许必要补证也不放开死循环。
- 阶段 14D：研究动作执行入口复用研究循环的隔离预算统计，移除执行前仍按全 Run token 二次拒绝动作的旧口径，保证规划门与执行门对同一预算作出一致决定。
- 阶段 14E：研究循环新增动作只依赖已完成的非写作研究任务，禁止依赖 `thesis_chapter`/`report_writing`；避免补证动作与随后更新的章节依赖形成研究↔写作环。
- 阶段 14F：章节全部通过后按实际正文总词数检查院校最小/最大范围；若越界，选择相对章节预算偏差最大的最新通过章，在保留事实、support 和边界的前提下定点扩展或压缩，最多两轮，仍不达标则 fail closed。
- 阶段 14G：论文章节返工链达到通用上限时，若当前章仅执行过一次，允许复用同一任务做一次携带完整正文和审稿清单的原地定点返工；第二次仍失败即终止，避免小修可解却被判死，也不创建无限返工链。
- 阶段 14H：末轮原地返工按任务总执行次数最多 3 次（首次交付 + 两次定点修复）收敛，容纳“修掉一组问题后审稿发现另一组问题”的章节；第三次仍未通过即终止，不按问题数量无限续命。
- 阶段 14I：原地返工在问题持续缩小时允许最多 5 次总执行，并强化“只改 target 段落、删除型意见直接删除原短语、不得用新事实性形容词替换”的最小编辑契约；第五次仍失败即终止，兼顾复杂章节收敛与防循环。
- 阶段 14J：论文章证据审查改为每批最多 6 段的穷尽审计，一次汇总整章全部重大越界，再进入只负责结构、章节职责、内部一致性和研究边界的整章审查；返工任务的完整旧正文不再重复进入审稿 task description，避免长上下文截断和“每轮只发现两处旧问题”的假性死循环。分段审计最多返回 18 个去重问题，仍受章节最多 5 次执行的总边界约束。
- 阶段 14K：分段证据审计复用章节 8192 token 上限，避免独立审稿的内部推理耗尽通用 4096 token 后返回空正文；详细审计连续结构失败时禁止降级为无逐项清单的二元判断，并在编排层只额外重试一次审核、不重做章节、不增加章节 attempt，第二次仍为传输失败则明确终止而不悬挂死循环。
- 阶段 14L：分段审计同时提供“当前绑定支持”与“全部可选冻结支持”：当前表述仍只按已绑定证据判定，但修复意见在确有直接蕴含时优先补绑 ID，而非机械删掉可验证事实；审稿模型返回 `overall` 时按引语和词项重合自动锚定具体段落。对首次完整分段审计时已被旧抽样审稿消耗完 5 次机会的章节，先复用现有正文重审，并只允许一次汇总清单修复（第 6 次执行），随后仍硬终止。
- 阶段 14M：分段审计不再在 `bound_support` 与 `available_support` 重复发送同一冻结支持；超过 6000 字符的实验/材料工件按字段路径、段落词项和数值重合构造最多 32 条的相关事实视图，保留原工件 ID、预注册、复现和与正文直接相关的参数/结果。真实计算课题的单个 7966 字符实验支持被压缩为约 3.8–4.1k，降低多批独立审稿的上下文、延迟和成本，不改变原始工件或最终追溯。
- 阶段 14N：院校最低/最高字数只在确定性全文装配门执行，单章不再按同一院校比例强制补字，而保留 `max(500, 章节预算50%)` 的结构性正文底线；解决结果章已超额、全文将达标时，引言仍被机械扩写并重新制造无支持机制的规则冲突。章节补全提示禁止新增机制、因果、效果解释和领域常识。已在旧比例门下耗尽 6 次的遗留章节仅允许一次携带完整分段清单的迁移修复，此后仍硬终止。
- 阶段 14O：穷尽段落审计后的返工不再让 LLM 重写整章；系统只接受两类单调外科变化：从原文精确删除审稿指向的现有完整句/短语，或补绑仓库中已存在的 supported claim、`experiment:*`、`brief:*` ID。审稿引语被截断时按原文句子词项重合选择删除目标，任何替换或新增正文字符都无入口；每章最多两轮外科审计修复且不增加 attempt。真实引言的 11 项问题可确定性完成 9 次原句删除和 2 次冻结支持补绑，修复后 876 词、结构门通过。
- 阶段 14P：修复“分段穷尽、结构却只保留 6 条”的反馈容量矛盾：独立审稿 schema 与归一化单批上限提升到 12 条，章节多批去重总上限提升到 36 条；非章节全局审稿提示仍要求最多 6 条。真实引言第一轮外科修复后暴露的 9 条均属于原批次第 7 条以后被旧上限截掉的旧问题，现可在同一批完整返回，不再靠下一轮发现。
- 阶段 14Q：外科修复优先逐一删除审稿意见中成对引号标出的精确原短语，同一问题的多个引语一次处理；仅在没有可定位成对引语时，才删除词项最匹配的完整原句。标点清理也只能删除重复逗号/空格，不生成替代正文。单章单调外科上限调整为 3 轮，以迁移前两轮旧整句算法；当前引言第 3 次精修可处理 12/12 项、0 项未解析，保留同句有证据内容后为 553 词并通过结构门，之后无第 4 次机会。
- 阶段 14R：单调外科修复最多 5 轮，每轮必须实际删除原文或补绑已验证 ID，否则立即停止；仍不调用写作模型，也不增加章节 attempt。事件持久化每轮问题数、未解析数、修改清单及修复前后词数，形成可审计的严格进展证据。真实引言第 3 轮复审仅余 1 个精确短语，删除后为 546 词且结构门通过；固定 3 轮会误杀可确定收敛的章节，因此放宽为有单调不变量的最多 5 轮，而不是无限续命。
- 阶段 14S：外科修复后确定性删除明显残缺原句，包括孤立 `The thesis.`、无谓语的 `work by … et al.`、删除谓语后留下的 `is,`/`but its.`、悬空 `aims to.` 等；句子切分保护 `et al. (year)` 和小数点，避免误删完整引文句。单章内部结构底线调整为 `max(300, 章节预算30%)`，章节仍须至少 3 段且每段至少 40 字符，院校全文字数硬门不变；当前引言清理后约 403 词、9 个实质段落，全文预计仍在 7000–8000 词范围。
- 阶段 14T：段落审计从“所有解释必须被来源逐字直接蕴含”升级为分层认识论协议：来源归因、数值、外部事实、因果和机制仍须直接支持；由多个绑定事实透明合成、无新增外部事实且明确限定范围的作者解释与 limitation 可以通过；transition 可组织论点但不得偷带事实；method 参数须与工件一致，选择理由可由冻结合同推出。审稿标识升级为 `paragraph_audit_v2`；旧 v1 失败章先只清理历史残句并重审，不消耗外科轮次或 attempt，避免把硕士论文削成文献事实列表。
- 阶段 14U：当 v2 独立证据与结构硬门全部通过、导师仅指出旧外科算法误删了有支持内容时，从任务描述中持久化的“上一版完整 chapter”解析导师点名的段落 ID，按原位置精确恢复原 `text`、段落类型与 `support_ids`；不得生成新字符，最多一次，并立即重新执行 v2 证据审计。真实引言可精确恢复 `intro_rw_1`、`intro_rw_2`、`intro_pilot_3` 三段，恢复后 697 词且结构门通过。
- 阶段 14V：执行循环的停滞指纹由“任务 ID + 状态”扩展为“任务 ID + 状态 + 持久化 attempt_count”；章节在同一轮中完成 `pending → 执行 → 审稿 → pending` 时，只要 attempt 单调增加就必须继续下一轮，不再误判为死循环。真实方法章已暴露该缺陷：第 5 次执行确实产生并审核了新章节，但旧指纹将返回 `pending` 误当作零进展。
- 阶段 14W：导师保真恢复是新的有界变更，不得因历史 v1 已用尽 5 次外科额度而永久无法处理；恢复事件存在、当前 v2 审计失败且常规额度已尽时，只允许一次独立的 `post_restoration_surgical_repair`，仍只能删除原文或补绑已验证 ID。同时兼容审稿模型的 `No bound support ...` 措辞，使其能确定性匹配并删除被指名的无支持原句。
- 阶段 14X：证据批次通过后，全局 v2 可能只剩工件单位、错放未来工作、近重复句、孤立标点或段落类型等编辑问题；新增最多一次 `global_editorial_repair`，仅执行工件验证后的引号内精确替换、删除未来建议句、删除相似度至少 0.8 的后出现重复句、清理标点及修改枚举内段落类型，不调用写作模型。真实引言可将工件确认的 `100 tokens/30-token` 精确改为 `100 characters/30-character`；真实方法章可删除 3 句错放展望和 1 句近重复、修正 1 个类型并清理 3 处标点，1047→956 词且结构门通过。
- 阶段 14Y：论文章节的分段 v2 审计通过后，全局审稿结果必须保留 `paragraph_audit_v2_global` 身份，不能被通用 reviewer 标签覆盖，否则确定性编辑器会把合法的全局问题误当成旧协议而拒绝。对升级前已持久化的通用标签，仅当同一任务已经存在 `epistemic_audit_migration` 或 `paragraph_audit_consolidation` 事件时兼容识别为 v2-global；普通任务和无迁移证据的旧审稿仍不得进入编辑修复。
- 阶段 14Z：大工件压缩不能丢失决定方法参数含义的合同；`fact_view` 之外固定保留最小 `frozen_method_contract`（策略、基线、检索配置、基准设计），使段落批次稳定看到窗口单位和参数。若段落已绑定 experiment 工件且冻结合同确认 characters，确定性外科不得执行审稿模型要求删除 character 单位的矛盾指令。全局编辑之后仅允许一次 `post_editorial_evidence_repair`，用于补绑遗漏工件和删除仍被点名的无支持短语；不增加 attempt、不生成替代正文。
- 阶段 14AA：单调外科新增工件验证的精确替换：只解析审稿清单中引号包围的 `old 改为 new`，仅当 old 确实存在于原段落且 new 的数值与单位可由冻结方法合同确认时原位替换；仍禁止自由生成。对升级前已经耗尽常规轮次、但 v2 因新 `frozen_method_contract` 才识别出参数问题的章节，仅允许一次 `frozen_method_contract_migration`，同时处理同批次可确定删除/补绑项。
- 阶段 14AB：一次性恢复和单调轮次不能依赖“最早 100 条事件”的截断视图；新增按 `run_id + task_id + event_type` 的数据库精确计数，`_has_task_event` 与 `_task_event_count` 均复用该查询。长章节即使累计超过 100 个执行、审核和恢复事件，也能看见最新迁移事件，既不会漏掉应执行的下一步，也不会重复消费一次性修复。
- 阶段 14AC：当分段证据、全局结构等科学硬门已经全部通过，导师仅指出返工清单漏删了某段中的逐字原句时，允许一次 `advisor_exact_cleanup`：反馈必须同时含当前段落 ID、删除/移除动词和在该段落逐字存在的引号内容，系统只删除该原文并重跑全部质量门；模糊建议、未点名段落或不存在的文本均不得触发，也不调用写作模型。
- 阶段 14AD：全局章节审稿升级为 v3：证据分段审计已通过后，不得仅因组织偏好拒绝有证据且有边界的内容；引言可简要预览关键数值、方法和局限，方法章可解释设计理由、复现控制、诊断和方法局限。只有事实矛盾、必要结构缺失、明显破坏论证的实质重复或大段完全离题内容可列 major；可选搬章/合并写入 summary。旧 v2-global 失败章仅执行一次 `global_scope_migration` 原文重审，不增加 attempt、不修改正文。
- 阶段 14AE：v3 全局审稿的 target 可能是语义标签而非段落 ID；外科修复在精确 ID 未命中时，只允许用 reason/required_change 中的引号原文在当前章节唯一定位段落，多段命中或无逐字命中均拒绝修改。常规轮次已尽的 v3-global 仅允许一次 `v3_global_exact_repair`，且必须同时具备删除动词和引号原文；用于删除被逐字指出的越界句，不生成替代表述。
- 阶段 14AF：当常规修复轮次已尽、v2 分段审计的本轮全部问题都只要求绑定明确写出的 `claim_`、`experiment:` 或 `brief:` ID 时，允许一次 `late_support_binding`；仍由外科服务核验 ID 必须处于 supported/冻结工件集合后才补入段落。该闭锁不改正文、不增加 attempt，若任一问题还要求无明确 ID 的生成或编辑则整轮不触发。
- 阶段 14AG：导师完成度审核不得推翻更强的冻结工件一致性门。仅在论文章节全部科学硬门已通过、当前正文使用 characters、冻结方法合同也确认字符窗口，而导师否决明确要求把 characters 改成 tokens 时，标记 `advisor_artifact_conflict_overridden` 并仲裁为通过；原意见完整保留用于审计。其他导师否决、模糊表达问题或工件未确认的单位争议均不覆盖。
- 阶段 14AH：升级前已经持久化的导师工件冲突不会自动经过 14AG；新增一次 `advisor_artifact_conflict_migration`，仅当旧章节科学硬门已通过且同一窄冲突检测成立时复用现有正文重跑审核。迁移不修改正文、不增加 attempt，并恢复被旧否决连带阻断的下游装配任务。
- 阶段 14AI：最终报告通过接地审计与科学质量门后，由已验证的完整论文统一结算 canonical `report_writing` 任务；中间摘要返工失败仍保留为审计轨迹，但不得让已生成的 `master_thesis` 与任务板终态互相矛盾。
- 阶段 14AJ：计算/工程真实项目已写入五范式登记表并由验收器从数据库、真实 LLM 用量、外部来源 passage、冻结输入哈希、可发布实验、最终报告哈希及重新计算质量门全链复核，结果无问题通过，五范式进度更新为 1/5。
- 阶段 14AK：社会科学首次真实运行发现跨轮检索误闭锁：模型把 `references_used` 返回为带筛选意见的对象时，系统误把整段 JSON 当作未知 source ID；导师明确要求扩检的返工仍冻结旧全文池；累计来源按入库顺序而非当前问题相关度送入有限上下文。现已只提取结构化记录中的既有 `source_id`、在扩检反馈下重新检索、按当前问题重排累计全文及 passage；仍要求来源已核验且 passage 可定位，自动研究轮次上限不变。
- 阶段 14AL：证据相关度不再让长全文中的偶然词重合主导排序；标题与出处承载主要领域命中，摘要/正文只能提供封顶的弱加分，学术提供商身份仅在通过领域重合过滤后用于优先。社会科学回放中的 QCD 物理报告因此不能凭长文本和 provider 身份挤到教育支出文献之前。
- 阶段 14AM：社会科学第二次真实负例暴露出补救动作的局部返工树可绕过研究循环总预算：3 个近似 `evidence_search` 动作各自生成返工链，879064 tokens 后仍为 0 条 claim。现将循环动作定义为单次可观测操作，审稿未通过即交回统一循环评估而不再派生返工；同轮同一工具只选最高优先级动作，预算识别根动作及遗留返工谱系。这样保留跨轮换策略能力，同时消除“补救动作内再补救”的指数膨胀。
- 阶段 14AN：结构化检索返回非空不再等于“证据已找到”。相关度过滤要求长问题至少在标题/出处命中 3 个主题词，超过 500 字符的畸形 issue-page 标题直接排除，正文只能弱加分；全文抓取拒绝 ZIP/Office 二进制、短重定向和低可读文本。完成核验与全文抓取后，若直接相关 grounded 来源仍少于冻结最低数，则每个根任务谱系只执行一次浏览器补检并将实际尝试写入协议；补检失败仍输出证据不足，绝不把元数据或乱码升级为 claim。
- 阶段 14AO：社会科学第三次真实负例 `run_9faaca0c` 验证了数值防幻觉门：独立审查拒绝了模型虚构的 `+1.42` 显著差异，并迫使结果回到已哈希工件中的 `-0.0266` 与跨零区间；但补证循环把 82 个 passage 塞入约 1.7 万 token 上下文，两轮均在 4096 输出上限处截断为非法 JSON，最终按连续无进展停在人工确认。现将带来源 URL 的上传附件登记为带 SHA-256、局部 locator 和透明 `user_attachment_integrity_verified` 状态的一手证据；Crossref/OpenAlex 优先使用其声明的 PDF/开放全文 URL；`[循环R]` 每个来源只发送一条最相关且最多 1800 字符的 passage，最多 5 条 claim，并复用 8192-token 结构化输出预算。全量测试 281 项通过，下一步重新执行社会科学真实案例。
- 阶段 14AP：社会科学第四次真实负例 `run_a807a765` 在限幅后能稳定完成两轮补证并形成 12 条受支持 claim，但把同一 World Bank 附件的初始 `attachment_source_*` 与后续 `source_*` 误算成两篇引用，报告 4 个来源而实际只有 3 个独立来源。现以 DOI、规范 URL、外部标识和书目信息计算唯一来源身份，附件首次入库即使用幂等标准 ID，循环的总来源数、claim 引用来源数与信息增益均按独立身份结算；“任务已完成”不再凭空获得最低信息增益。循环检索只继承冻结研究问题，Crossref 查询去除 Boolean/长动作噪声并限制为 16 个去重主题词，从而让无新增引用的动作被有界判为负结果，禁止重复来源凑足论文门槛。
- 阶段 14AQ：社会科学第五次真实负例 `run_1b7b92e6` 中，方法任务的第二个返工版本已经通过，但较旧的 `need_revision` 兄弟被停滞终止器误判为整族失败，进而把结果与论文任务错误标成前置失败；结果分析返工又在通用 4096-token 上限处截断为无可见 JSON。现每轮审核后立即归档已被通过版本取代的返工兄弟，停滞终止前再次检查同族根任务或任一返工是否已完成，已完成则只归档旧草稿、绝不传播失败；结果分析与最终报告复用 8192-token 长结构预算，并要求 summary 和列表限幅、不得复述上游 JSON。返工次数仍受原有上限约束，真实科学否决不会被覆盖。
- 阶段 14AR：同一真实运行进一步发现，任务执行阶段会在导师审核前按 passage 链把 claim 直接标成 `supported`，因此被审核拒绝的偏题补证仍暂时把受支持结论从 2 条抬到 6 条。现 claim、hypothesis 与 uncertainty 以“产出任务 ID + 规范化内容”的确定性 ID 隔离暂存，初始状态分别为 draft/staged/staged；只有该任务自己的审核决定通过后，才重新计算 claim 支持并把假设、不确定性晋升为 proposed/open。研究循环每次快照还会按持久化审核决定修复旧库状态；`run_1b7b92e6` 回放后偏题结论全部降回 draft，真实状态恢复为 2 条 supported、1 个独立引用来源、不可报告。
- 阶段 14AS：为使升级前已被旧返工族误杀的真实运行可继续，服务启动恢复与既有运行执行入口会检查“已有通过返工且根任务已完成”的任务族；其后代若且仅若以 `前置任务失败` 被旧错误终止，则清除旧审核阻塞并恢复 pending。实验复现失败、科学硬门失败、传输失败等真实失败不恢复。该迁移让 `run_1b7b92e6` 能复用已冻结数据、证据和通过的方法计划继续验证 8192-token 结果分析，而不重跑整个项目或掩盖真实否决。
- 阶段 14AT：真实恢复后结果根任务虽已回到 pending，却仍被同族一个 `failed` 的 JSON 截断返工永久占用“进行中返工”锁；任务图因此没有任何 ready 任务。现返工锁只统计 pending/assigned/blocked/running/waiting_subagent/waiting_review/need_revision 等非终态分支，failed/completed/archived 历史仅保留审计，不再阻止明确恢复的根任务。R2 补证已按两轮上限停在人工确认，状态仍为 2 条 supported、1 个独立引用来源，未借恢复降低论文门槛。
- 阶段 14AU：第五次运行的 Crossref 实际查询把收入组、比较方式和描述统计脚手架全部混入主题，首批结果因此被税收占 GDP、温室气体和一般政府支出占据；相同服务对 `government education expenditure GDP` 则直接返回教育支出占 GDP 条目。现提供商层通用去除 percent/share、country/economy/group、comparative/cross-country/descriptive/statistics 与收入层级等比较脚手架，只保留研究对象、构念与核心指标；不硬编码教育、国家、机构或具体文献。完整原查询仍写入检索协议并用于下游相关度审查，精简仅影响 Crossref 候选召回。
- 阶段 14AV：第六次真实运行 `run_a9bce69c` 的定量结果返工已通过，8192-token 路径成功返回合法 JSON，均值差、区间、样本量和材料哈希均与确定性工件一致；但紧随其后的循环快照把“无 passage、由分析工件支持”的 claim 再交给文献链接评价，错误降回 draft。现重启回放对审核通过的任务复用正常审核的同一晋升顺序：先评价 passage 链，再按已验证分析/实验工件晋升，最后开放已审核假设与不确定性；未审核任务仍统一降级。这样工件 claim 不会被文献规则覆盖，且文献 claim 也不能绕过 passage 门。
- 阶段 14AW：第六次社会科学运行的结构化检索已找到 OECD 教育支出条目与可追溯 DOI，但浏览器补检丢弃了这些已排序候选，重新从泛搜索引擎开始，有限步数被重复搜索和指标网页消耗。现在补检优先携带当前问题下已通过书目核验、按相关度排序的学术候选 URL，跳过用户附件并按浏览器候选上限截断；浏览器须先直接打开这些页面，仍只能返回页面可见证据，后续全文获取、来源核验与 passage 接地门均不降级。
- 阶段 14AX：社会科学定量观察运行已产生审核通过、输入哈希与方法检查齐全的确定性分析工件，旧研究循环却只按 `research_type=empirical` 要求可发布计算实验，并对观察性假设索要领域实验模板和标注数据。现由方法画像决定结果硬门：computational/experimental 仍必须有可发表且复现通过的实验；其他已声明方法族必须有通过本任务审核、全质量门和非模拟独立审查的方法专用分析工件；被通过返工取代的旧根任务不重复计数。研究循环和最终科学报告门共用该判定，缺少方法工件时只派发有界分析动作，不再伪造不属于该范式的实验。
- 阶段 14AY：第六次运行回放在正确识别定量分析工件后，仍把 8 条来自审核拒绝任务或已被成功返工取代根任务的 draft claim 算入覆盖率并逐条派发补证，其中包含已被结果返工纠正的错误叙述。现以任务输出中持久化的 graph ID 与该任务自己的最新审核决定构造审核作用域：已通过对象可进入研究循环与论文；未通过、未审核和被返工取代对象仍保留数据库审计记录，但不再进入有效 claim/假设/不确定性集合；不属于任务 graph 的旧手工记录保持兼容。这样既不删除负例，也不会让被否决的幻觉成为系统永远必须“证明”的新目标。
- 阶段 14AZ：第七次真实社会科学负例 `run_d7defb11` 证明 DOI 直达已生效，但候选中两张“政府/地方支出占 GDP”通用图因命中高频词而挤入教育主题；三个 OECD DOI 又直接触发 XLS/XLSX 下载，浏览器无法读取且页面被 Cloudflare 阻断。浏览器自报成功并从下载文件名推断 evidence，但其独立 judge 明确 verdict=false；运行因已不可能达到五个独立全文来源而主动取消，仅保留负例审计。现浏览器发现或核验历史只要存在独立 judge 且判定失败，就丢弃该轮自报结构，禁止失败证据进入来源库或再次浏览器核验。直达候选复用 Crossref 精简查询提取通用主题核心，必须同时命中候选集中最稀有的查询核心词和至少三个核心词，不使用教育或具体学科硬编码。
- 阶段 14BA：第七次负例的 Crossref 前 12 条几乎全被 `component` 类图表子对象占据，即使某些标题相关，也通常只有下载链接、无作者和可读页面，不能替代学术文献。现按 Crossref 官方 REST 接口的书目查询语义使用 `query.bibliographic`，一次最多扫描交付上限四倍且封顶 100 的候选窗口，先返回 journal article、report、chapter、book、dataset 等实质 work，再用 component/reference-entry/peer-review 补足；最终仍只交付原配置数量。OpenAlex、arXiv 和 Semantic Scholar 同样复用已有查询精简器，避免收入组、比较方式和长任务语言污染学术召回。后续仍必须通过标题主题过滤、身份核验、真实全文获取和 passage 接地才能形成 claim。
- 阶段 14BB：第八次社会科学负例 `run_61b5b417` 已召回 journal article、book chapter、book 和 posted content，浏览器也真实打开 OSF 预印本并提取页面摘要，证明 14BA 有效；但全文槽位的第二个来源是“温室气体排放、GDP、高/中低收入组”比较文章，只因原问题中的收入组和 GDP 脚手架而入选，最相关的教育支出文章则排到全文上限之后。现最终相关度同时计算完整冻结问题与精简书目核心；当候选中至少两篇同时命中三个核心词时，只保留达到核心门的来源，否则保留原宽松排序以容纳同义改写。查询精简器同时去掉 frozen/snapshot/reported/dissertation/contract 等工作流语言，不增加任何学科关键词。运行因仍不足五个独立全文来源而主动取消，不进入论文生成消耗。
- 阶段 14BC：第九次社会科学负例 `run_00b43e4a` 的冻结合同明确是 quantitative 横截面观察比较，随机导师拆解却额外生成 `Experiment Design for Descriptive Comparison`；旧方法能力转换又把 quantitative 列为实验可执行族，因而保留了与已有 data acquisition 重复、且会误导后续审批的伪实验任务。现仅 computational、experimental 和 design_science 保留 `experiment_design`；quantitative 与其他非实验族将该标签转为真实材料获取，若计划已有 `data_acquisition` 则直接丢弃这条重复任务。这是确定性方法合同纠偏，不依赖学科关键词或导师再次判断。
- 阶段 14BD：第九次运行的初次全文抓取仍接纳了 `The analysis of the proportion of Chinese national health expenditure in GDP`；其作品标题只命中 expenditure/GDP，但会议名含 `Innovative Education`，旧硬门把 title 与 venue 等权合并后误判为三词命中。现只有作品标题词可计入相关度硬门；venue 最多两词、每词 0.1 弱加分，摘要/正文最多两词、每词 0.25 弱加分，二者都不能补齐标题门槛。这一字段责任分离适用于所有学科，同时保留了出版地和摘要对同分候选的有界排序作用。
- 阶段 14BE：五范式真实验收中，计算/工程与系统综述 runner 都提供冻结种子文献，社会科学 runner 却只提供数据，使每次验收都把主要预算消耗在被 CAPTCHA 干扰的开放搜索，与其他范式的起点不对等。现 social 基准冻结 6 个已实际打开核对的起始来源：World Bank/UNESCO `Education Finance Watch` 2021、2023、2024 三份独立报告，OSF/MPRA 教育支出论文，Nepal 期刊论文，以及 Our World in Data/UNESCO 指标说明。种子只包含真实标题、作者、年份、DOI/URL；仍必须重新下载正文、哈希、切分 passage、通过 claim 蕴含与独立审查，仅元数据不计入论文门槛。provider 搜索仍保留作为扩展和反证途径。
- 阶段 14BF：第十次社会科学负例 `run_2aeb3938` 正确拆成文献、方法、数据、描述统计和论文写作五项且没有伪实验，但 6 个冻结种子在全文抓取前先经过标题相关度硬门；三份标题简短的 `Education Finance Watch` 被删除，另三份又受浏览器 SSL/轨迹审判影响，最终只有附件与一篇无关 arXiv 页面形成全文，证明“优先种子”在旧顺序下并未真正生效。现仅对用户明确给出的冻结起点保留原顺序并先下载正文；系统对下载文本计算哈希，以正文前部核对标题、以全文核对 DOI，只有身份匹配才获得引用资格并跳过冗余浏览器身份检查。进入研究证据池时还会用实际 passage 再做主题重合检查，普通检索结果继续执行严格标题硬门，因此既不会因通用报告标题误删真实资料，也不会盲信无关种子。MPRA 工作论文同时移除不属于该版本正文的 OSF DOI，避免把不同托管版本强行绑定。
- 阶段 14BG：第十一次社会科学负例 `run_ad816578` 已证明种子确实在检索前被下载：2024 报告、MPRA 和 OWID 直接形成三份哈希正文，2021/2023 报告则触发 `incorrect startxref` 后退回浏览器。对照浏览器下载记录发现两份 PDF 均能被同一 `pypdf` 完整读取（分别约 5.6 万和 11.8 万字符），真实故障是后端固定只读取响应前 4 MiB，截断大 PDF 后才造成伪损坏。现网络全文使用独立的 `FULLTEXT_MAX_DOWNLOAD_MB`（默认 25 MiB）做有界完整读取：先检查 `Content-Length`，未知长度时最多读取上限加一字节并显式拒绝超限，绝不再把截断内容交给解析器。这一修复覆盖所有学科的大型论文和报告，不增加依赖，也不解除资源边界。
